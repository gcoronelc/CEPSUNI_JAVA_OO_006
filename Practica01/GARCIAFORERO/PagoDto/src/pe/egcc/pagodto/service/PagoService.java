package pe.egcc.pagodto.service;

import pe.egcc.pagodto.dto.PagoDto;

/**
 *
 * @author Gustavo Coronel
 * @blog http://gcoronelc.blogspot.com
 * @email gcoronelc@gmail.com
 */
public class PagoService {

  
 /* public PagoDto procesar(PagoDto dto){
    
    // Variables
    double ingresos, renta, neto;
    
    // Proceso
    ingresos = dto.getDias() * dto.getHoras() * dto.getPago();
    renta = 0.0;
    if( ingresos > 1500 ){
      renta = ingresos * 0.08;
    }
    neto = ingresos - renta;
    
    // Reporte
    dto.setIngresos(ingresos);
    dto.setRenta(renta);
    dto.setNeto(neto);
    
    return dto;
  }*/
  
}
